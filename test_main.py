import pytest
from io import StringIO
# from .main import main

# def test_main(monkeypatch):
#     value = 'test sys stdin'
#     monkeypatch.setattr('sys.stdin', StringIO(value))
#     assert main() == value
